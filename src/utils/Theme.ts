import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
    colors: {
        heading: {
            'light': '#575A89',
            'dark': 'yellow'
        },
        body: '#ffffff',
    }
})

export default theme